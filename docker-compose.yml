version: "3"
services:
  alpine-base:
    build:
      context: .
    image: augustash/alpine-base:4.0.0
    labels:
      - "com.augustash.description=August Ash Alpine Base Image"
      - "com.augustash.development-only"
